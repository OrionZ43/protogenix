# Журнал задач Claude

Хронология работы Claude Code в этом репозитории: дата, что сделано, почему, что осознанно не трогал. Новые записи — сверху.

- Правила по темам живут в `.claude/rules/`. Сюда — только история и ссылки на них.
- `.jules/` — журналы уроков Jules (Bolt, Sentinel). Claude их читает, но не пишет туда.

Формат записи:

```
## ГГГГ-ММ-ДД — краткое название задачи
**Что:** …
**Почему:** …
**Осознанно не трогал:** …
```

---

## 2026-09-12 — Страница «Инфо» переделана

**Что:**
- **Было:** одна стеклянная карточка — логотип, плашка «v1.0.0 — Плеер нового поколения» и мелкие подчёркнутые ссылки. Версия вписана руками, после первого же релиза она бы врала.
- **Стало — «неон-витрина»** (выбор Orion):
  - шапка: неоновый логотип, настоящие версия и сборка (`package_info_plus`) с платформой, статус обновлений с кнопками «Проверить» / «Обновить» и прогрессом, ссылка «Все версии и что нового»;
  - «Медиатека»: число треков, длительность, место на диске (по путям из базы, в отдельном изоляте), самый частый исполнитель;
  - «Данные»: папка данных и «Открыть папку» на десктопе; на Android — пояснение, что данные удаляются вместе с приложением; «Телеметрии нет» и что именно уходит в сеть;
  - «Благодарности»: источники текстов и библиотеки, «Лицензии открытого ПО» (`showLicensePage`);
  - внизу — ссылки на сайт, GitHub и Telegram кнопками (`Z43BrandingBadge` на общем `ChipButton`);
  - на широком экране «Медиатека» и «Данные» идут в две колонки.
- **Обновлялка:**
  - `UpdateChecker.checkDetailed` отличает «новее нет» от «проверить не удалось». Раньше оба случая давали `null`, и страница без сети писала бы «последняя версия»;
  - `UpdateNotifier.checkNow()` — для кнопки; при сбое повторной проверки найденное обновление не теряется;
  - `check` остался обёрткой: баннер и старые тесты не менялись, добавлен тест на исходы.
- Карточки без `BackdropFilter`: страница прокручивается (`performance.md`, правило 5).
- Скриншот превью в двух ширинах (400 px и широкая) нашёл баг: фон (`AnimatedBackground` — `Stack`) брал размер содержимого, и под короткой страницей на высоком окне оставалась чёрная полоса. Исправлено на самой странице (`SizedBox.expand`).
- **Проверки:** `flutter analyze` — 7 замечаний (базовый уровень); тесты — 86 пройдено, 2 пропущено.

**Почему:** Orion: «страница инфо вообще не нравится».

**Осознанно не трогал:**
- Пока релиз не опубликован, статус — «Не удалось проверить обновления»: манифест на GitHub отдаёт 404.
- `AnimatedBackground` не менял: он стоит под всеми экранами. Та же чёрная полоса возможна и на других экранах, если они короче окна, — отдельная проверка.
- На телефоне страницу не смотрел — только превью на Windows шириной 400 px.

## 2026-09-12 — Эквалайзер заработал, меньше работы в фоне, сборка под VS 2026, видео для сайта

**Что:**
- **Сборка под Visual Studio 2026** (у Orion — 2026 Insiders, в CI — `windows-latest`):
  - для `permission_handler_windows` — макрос `_SILENCE_EXPERIMENTAL_COROUTINE_DEPRECATION_WARNINGS` в `windows/CMakeLists.txt`: в MSVC 14.51 `<experimental/coroutine>` уже ошибка STL1011;
  - `audiotags` удалён по решению Orion: он не использовался, а с CMake 4 не распаковывал свой архив через symlink. Вместе с ним `pub get` убрал `flutter_rust_bridge` и ещё 4 транзитивных пакета;
  - CI снова на `windows-latest`; локальная release-сборка Windows прошла.
- **Имена файлов в релизе без версии** (`Protogenix-Setup.exe`, `protogenix-android-*.apk`): ссылка `releases/latest/download/<имя>` всегда ведёт на свежий релиз, ею пользуется сайт. Описание v1.0.0 и `release.md` поправлены.
- **Эквалайзер** — решение Orion: «раз на сайте написано — делаем». Раньше шторку не открывала ни одна кнопка, а ползунки меняли только состояние. Теперь:
  - эквалайзер подключён к звуку;
  - настройки сохраняются в `equalizer.json`;
  - кнопка — в верхней панели плеера, только Android.
- **Фоновая работа** (аудит агентом, список — `performance.md`):
  - баг Orion: телефон вибрировал на каждой строке текста и со свёрнутым плеером. Теперь вибрация только в виде текста и только когда приложение на экране. С открытым текстом было 3 щелчка на строку, стало 2;
  - пока приложение свёрнуто, поиск текста (до ~30 запросов на трек), расчёт активной строки и цвета обложки ждут возвращения (`app_visibility.dart`);
  - цвета обложки — по картинке 96 px (параметр `size` у palette_generator ничего не уменьшал), без пересчёта для той же обложки; устаревший результат не перетирает свежий;
  - тикеры скрытых вкладок заглушены `TickerMode`;
  - убрана подписка на `bufferedPositionStream` — `buffered` никто не читал.
- **Видео для сайта:** режим демо (`lib/app/demo_mode.dart`, включается только флагами сборки) и `tool/record_demo.ps1`. Скрипт собирает приложение, пишет окно через ffmpeg (Windows Graphics Capture, без курсора), берёт звук из файла трека и сводит его по парам «время — позиция», которые пишет приложение (разброс 1 мс).
  - Записан «2WEI & Edda Hayes — BURN», 1:03–1:30 (выбор Orion): 27 с, 1264×812, 30 кадров/с, 1,6 МБ.
  - Лист кадров проверен глазами: развёрнутый плеер, текст по слогам, время в плеере с 01:03.
- **Проверки:** `flutter analyze` — 7 замечаний (базовый уровень); тесты — 85 пройдено, 2 пропущено (3 новых — `eq_settings_store_test.dart`).
- **Правила:** `known-issues.md` (эквалайзер, VS 2026), `dependencies.md` (audiotags), `release.md` (имена файлов), `performance.md` (аудит), `lyrics.md`, `CLAUDE.md`.

**Почему:** запросы Orion: ссылки и описание на сайте, видео с караоке, рабочий эквалайзер, вибрация в свёрнутом плеере и лишняя работа в фоне на слабых телефонах. А CI на `windows-latest` был красным на всех PR.

**Осознанно не трогал:**
- Эквалайзер, вибрацию и фоновую работу на настоящем телефоне не проверял — это за Orion.
- Остаток аудита — отдельной задачей, список в `performance.md`: позиция в отдельном провайдере, мини-плеер, медиасессия на каждое событие буфера, фон на паузе, разбор текста на UI-изоляте.
- В видео — фрагмент чужой песни со звуком. Orion предупреждён про авторские права и DMCA, решение его.

## 2026-09-12 — Ежедневная проверка YouTube на машине Orion; первые PR от Dependabot

**Что:**
- 11 коммитов запушены в master. Через минуту Dependabot открыл первые PR: три — версии GitHub Actions, один — общий для pub.
- В общем PR для pub ломающие переходы оказались рядом с безобидными:
  - ломающие: `flutter_riverpod` 2 → 3, `riverpod_annotation` и `riverpod_generator` 2 → 4, `just_audio` 0.9 → 0.10, `audio_session` 0.1 → 0.2;
  - безобидные: `dio`, `path_provider`, `window_manager`, `audio_service`.

  Правило «без semver-major» Dependabot для pub в группе не применил, а переход 0.9 → 0.10 он считает минорным.
- `.github/dependabot.yml`: эти пакеты закрыты явно (с версиями), кодогенерация не обновляется вовсе, в общий PR идут только минорные версии и патчи.
- `tool/youtube_health_check.dart` и `.ps1` — ежедневная проверка на ПК Orion:
  - метаданные и первые 256 КБ двух его треков через клиентов импорта по порядку, плюс поиск;
  - лог — `%LOCALAPPDATA%\Z43 Studios\health\youtube.log`, при поломке — уведомление Windows;
  - задание Планировщика «Protogenix YouTube check» зарегистрировано: каждый день в 12:00.
- Проверено:
  - ручной запуск и запуск из Планировщика — оба трека через VISIONOS, поиск работает, код 0, около 20 с;
  - `dart analyze` — без замечаний;
  - тестовое уведомление вызвано без ошибок; видно ли его на экране, подтверждает Orion.
- Правила: `dependencies.md`, `known-issues.md`, `CLAUDE.md`.

**Почему:** решение Orion — проверять YouTube на его ПК. На серверах GitHub YouTube режет IP дата-центров, и были бы ложные тревоги. Проверка ловит то, чего не видит Dependabot: YouTube ломает клиентов и без новой версии библиотеки.

**Осознанно не трогал:**
- Старый общий PR от Dependabot не закрывал: после обновления конфига Dependabot должен сам его пересобрать или закрыть. Сливать его нельзя.
- PR с новыми версиями GitHub Actions ждут зелёного CI и решения Orion.
- Проверка повторяет логику скачивания, а не вызывает `ImporterService`: тот тянет Flutter и базы.
- При запуске задания может мелькнуть окно консоли — так ведёт себя `-WindowStyle Hidden` у powershell.exe.

## 2026-09-12 — Автопоиск новых версий библиотек: Dependabot, CI, `-Bump` в скрипте релиза

**Что:**
- `.github/dependabot.yml`:
  - pub — каждый день в 06:00 по Минску;
  - `youtube_explode_dart` отдельным PR (группа `youtube`), остальное — одним (`other`);
  - крупные версии (semver-major) не предлагаются, `audiotags` не трогается;
  - раз в неделю — версии GitHub Actions.
- `.github/workflows/ci.yml` — на каждый PR и push в master, без секретов:
  - `flutter analyze`: падает, если замечаний больше базового уровня (7);
  - тесты и сборка Windows на `windows-latest`; сборка лежит в артефактах 7 дней — чтобы проверить обновление живым скачиванием, не собирая у себя;
  - APK arm64 (без release-ключа, подпись debug).
- `tool/release.ps1 -Bump patch|minor|major`:
  - следующая версия, N на единицу больше опубликованного;
  - после успешной сборки — коммит «release: vX.Y.Z» (`pubspec.yaml` и описание), с `-Publish` — push и релиз;
  - упала сборка — `pubspec.yaml` возвращается как был;
  - заодно скрипт требует, чтобы описание лежало в файле своего тега, `vX.Y.Z.md`.
- Проверено на временной копии репозитория (git worktree) с подменённым `flutter`:
  - подсчёт patch, minor и major;
  - отказ `-Bump` вместе с `-AllowDirty` и отказ при файле описания от другой версии;
  - правка и возврат `pubspec.yaml`: меняется только строка версии, CRLF сохраняется.

  Тест нашёл баг в новом коде: путь к описанию сравнивался строкой и не совпадал, если в одной из строк короткое имя папки (8.3). Теперь относительный путь спрашивается у git.
- Правила: `dependencies.md` (раздел про автопоиск), `release.md` (`-Bump`, зелёный CI перед релизом), `known-issues.md` (базовый уровень в CI), `CLAUDE.md` (команды, CI).

**Почему:** договорились с Orion: новая версия библиотеки (прежде всего youtube_explode_dart) должна обнаруживаться сама и проверяться CI, а релиз — выходить одной командой. Ключи подписи в CI сознательно не отдаём: релизы собираются и подписываются только на машине Orion.

**Осознанно не трогал:**
- CI ещё ни разу не запускался — заработает после push. Первый прогон покажет, проходят ли тесты на чистой машине.
- Коммит и push через `-Bump` вживую не запускались: тест останавливался на сборке. Первый релиз с ним проверить глазами.
- Автомерж PR от Dependabot не включал: живая проверка скачивания на своих треках остаётся за человеком.
- Ежедневную проверку «скачивание с YouTube ещё работает» не делал — вопрос к Orion.

## 2026-09-12 — Запасной путь через Invidious/Piped удалён

**Что:**
- Удалён `lib/core/services/invidious_proxy_service.dart` со всеми вызовами.
- `ImporterService`:
  - YouTube по ссылке: если ни один клиент не отдал аудио — сразу понятная ошибка; некорректная ссылка — «Некорректный YouTube URL», как и раньше;
  - Spotify и Яндекс Музыка: поиск на YouTube вынесен в `_searchYouTube` (выбор «- Topic», VEVO, «(Official Audio)» прежний). Не скачался трек Spotify — ошибка, трек из плейлиста Яндекса — пропуск;
  - **исправлено попутно:** успешный импорт из Spotify не присылал статус `done` (его присылала только ветка Invidious), поэтому шторка импорта не обновляла медиатеку и очередь и оставалась на «Сохранение в библиотеку...»;
  - убраны фразы «слома 4-й стены» (`randomPhrase`).
- `TrackModel`: `_tryDirectOrInvidious` → `_tryDirectYouTube`. Если YouTube не отдал поток — тишина-заглушка вместо «прокси» с зашитым мёртвым адресом `invidious.io.lol`. Бросать исключение нельзя: `loadPlaylist` ждёт все источники очереди через `Future.wait`.
- Шапки `importer_service.dart`, `track_model.dart` и `search_screen.dart` переписаны: они описывали прокси, `_PlayButton` и `_ProxyBanner`, которых уже нет.
- Коммит Jules `2babf04` (2026-06-28) обещал удалить этот путь целиком, но убрал его только из поиска — записано в `known-issues.md`.
- Незакоммиченная переделка Orion `invidious_proxy_service.dart` (2026-06-28) не удалена, а отложена в `git stash` («WIP invidious_proxy_service (28.06)…»).
- Проверки: `flutter analyze` — 7 замечаний (было 8: ушло одно `experimental_member_use` вместе с `LockCachingAudioSource` из ветки Invidious); тесты — 82 пройдено, 2 пропущено.
- Правила: `known-issues.md` (раздел про Invidious, базовый уровень), `CLAUDE.md` (Architecture, Playback, Import).

**Почему:** решение Orion. Публичные инстансы Invidious и Piped закрыли API (проверка 2026-09-12): путь только добавлял ожидание при сбое и сообщения «через прокси», которые ничего не значили.

**Осознанно не трогал:**
- Живой импорт после удаления не запускал: само скачивание (`_tryDownloadWithClient`) не менялось и проверено живым тестом раньше в этот день. Импорт из Spotify стоит проверить руками — там поменялось поведение шторки.
- Яндекс Музыка шлёт промежуточное «Создание плейлиста...» со статусом `done`. Шторка смотрит статус только в конце, на результат это не влияет.
- Неиспользуемый `dart:math` в `importer_service.dart` — базовый уровень, попутно не чинил.
- Stash с переделкой: удалить его или оставить — решение Orion.

## 2026-09-12 — Третий источник текстов: Kugou (тайминги по словам)

**Что:**
- Проверка источников с машины Orion (печатались только метаданные):
  - Kugou работает;
  - Genius отдаёт только текст без таймингов;
  - Musixmatch на любой запрос отвечает одним и тем же чужим треком — похоже на защиту от ботов;
  - YouTube Music из Беларуси вкладку текста не отдаёт; с регионом US вкладка есть, но простым запросом сам текст не пришёл.

  Решение Orion — подключить Kugou.
- `kugou_krc.dart`: расшифровка KRC (base64 → «krc1» + XOR с известным ключом → zlib) и перевод в YRC. Время слов в KRC отсчитывается от начала строки, в YRC оно абсолютное.
- `kugou_provider.dart`:
  - поиск только по «артист - название»: свободный текст даёт 0 кандидатов (проверено);
  - в запрос уходит длительность;
  - берутся только правдоподобные кандидаты, не больше двух скачиваний;
  - копии одной записи под разными id отсеиваются.
- `LyricsService`: Kugou — третий провайдер. Запросы по полям несут длительность и `relevance`, на втором этапе уходят и для других прочтений названия. `splitArtists` понимает «、».
- Тесты: `kugou_krc_test.dart` (синтетический KRC со словами-заглушками), разделитель «、» в `lyrics_text_test.dart`.
- Живой замер:
  - итог: слоги у 9, строки у 13 (было 7 и 15); по-прежнему 22 из 23; в среднем 1,3 с;
  - «Кино — Группа крови» теперь со слогами от Kugou;
  - для The Hills, Believer, Blinding Lights и Numb у Kugou есть такие же версии — запасные к NetEase;
  - у «Starboy» (230 с) у Kugou только версии на 241 и 257 с — выбор остался за строками LRCLIB с точной длительностью;
  - нишевого русского рэпа (5opka, MellSher) у Kugou нет.
- Проверки: `flutter analyze` — 8 замечаний (базовый уровень); весь набор тестов — 82 пройдено, 2 пропущено.
- Правила: `lyrics.md` (раздел про Kugou), `CLAUDE.md`.

**Почему:** решение Orion — больше таймингов по словам у западной музыки и страховка к LRCLIB и NetEase.

**Осознанно не трогал:** Genius, Musixmatch и YouTube Music — результаты проверки записаны выше, в код не подключены.

## 2026-09-12 — Поиск текстов переделан: кириллица, выбор «та же песня → формат», длительность, slowed

**Что:**
- Замер на медиатеке Orion (15 треков) и 8 контрольных песнях. Печатались только метаданные вариантов, без текстов.
  - **До:**
    - текст чужой песни у 3 треков из 15 («Пятнистый ягуар», «42», инструментал «Студзянки»);
    - кириллица стиралась при сравнении (`\w` в Dart — только латиница), и все русские варианты получали одинаковые 5 баллов;
    - версия по слогам или строкам терялась в 5 случаях;
    - не та версия (slowed, клип с интро, ремикс) — в 3–4 случаях;
    - в среднем 3,2 с на трек.
  - **После:**
    - текстов чужих песен 0;
    - «Пятнистый ягуар» и «42» находятся (LRCLIB, точная длительность);
    - у «Студзянок» — «не найдено»;
    - slowed-версия получает текст оригинала с таймингами ×1,146;
    - клип Starboy (273 с) и live-версия (260 с) — записи с той же длительностью;
    - в среднем 1,2–1,5 с на трек. Итог: 22 из 23, двадцать третий — инструментал; слоги у 7, строки у 15.
- Новые файлы:
  - `lyrics_text.dart` — сравнение строк: кириллица, «ё» = «е», транслит, артисты по одному;
  - `track_query.dart` — разбор грязных названий трека и найденных записей;
  - `lyrics_matcher.dart` — «та же песня», потом длительность, потом формат; «не найдено» вместо чужой песни; растяжение для slowed/sped up; `timingsReliable`;
  - `lyrics_timing.dart` — растяжение таймингов и пометка `[pgscale:…]`.
- Переписаны:
  - `LyricsService` — два этапа, точный и широкий; `findBest`;
  - `LrcLibProvider` — `/api/get`, поиск по полям и `q`, повтор на 503;
  - `NetEaseProvider` — текст только для правдоподобных песен (до трёх), повтор только на 429/503, все артисты;
  - `LyricsQueryBuilder`;
  - `KaraokeNotifier` — длительность трека, только «та же песня», растяжение, без синхронизации при чужой версии;
  - ручной поиск — длительность, растяжение, пометка «точно эта песня», понятная ошибка вместо `$e`;
  - `ScoredLyric` — `isConfident`, `timeScale`, `timingsReliable`.
- Тесты:
  - 26 юнит-тестов на реальных случаях замера (только метаданные);
  - живой замер `test/live/lyrics_bench_test.dart`, по умолчанию пропускается; запуск — `--run-skipped`.
- Проверки: `flutter analyze` — 8 замечаний (базовый уровень); `flutter test` — 79 пройдено, 2 пропущено.
- Правила: `lyrics.md` переписан, `CLAUDE.md`.

**Почему:** жалоба Orion — поиск неточный, строки выбираются, даже когда нашлась версия по слогам, часть текстов не находится.

**Осознанно не трогал:**
- Новые источники текстов (Musixmatch и т. п.) — второй этап, серый вариант, решение Orion.
- Мёртвый код `lyrics_selector_sheet.dart` / `LyricsSelectionLogic`.
- То, что «не найдено» кэшируется на сессию, и разбор на UI-изоляте.

**Замечено:** у «Starboy» (Topic) NetEase сейчас не отдаёт версию по слогам ни по «The Weeknd Starboy», ни по «Starboy» (проверено запросами 2026-09-12). Выбраны строки LRCLIB с точной длительностью. В первом замере NetEase отдал другую запись: его выдача нестабильна.

## 2026-09-12 — Скачивание с YouTube снова работает: клиент visionOS, youtube_explode 3.1.0, починка файлов

**Что:**
- `lib/core/services/youtube_clients.dart` (новый):
  - клиент visionOS из неслитых PR #390/#391 youtube_explode_dart;
  - порядок клиентов `kYoutubeClientFallbackOrder`: visionOS, androidVr, ios, android, mweb;
  - `youtubeClientName` — имя клиента в логах вместо «Instance of 'YoutubeApiClient'».
- `ImporterService`:
  - порядок клиентов берётся из `youtube_clients.dart`;
  - `_tryDownloadWithClient` качает во временный `*.part` со своим именем, закрывает его до удаления и переименовывает только целиком скачанный. Больше нет файлов по 0 байт и конфликта двух импортов одного трека;
  - вместо «YouTube заблокирован, Invidious тоже недоступен» с сырым исключением — «Не удалось скачать трек» с понятным текстом. Сырой `e.toString()` убран и из ошибок Spotify и Яндекс Музыки (правило 3 из `security.md`).
- `TrackModel._getManifest`: воспроизведение YouTube-трека без файла сначала идёт через visionOS, при ошибке — через клиент по умолчанию. Шапка файла больше не утверждает, что Invidious «полностью устраняет» 403.
- youtube_explode_dart 3.0.5 → 3.1.0 (`^3.1.0`). В `pubspec.lock` поменялся только этот пакет, audiotags 1.1.3 на месте.
- Проверки:
  - `flutter analyze` — 8 замечаний, базовый уровень;
  - `flutter test` — 56 пройдено, 1 пропущен. Из них 3 временных живых теста: оба трека Orion (`jYvm-77C7XY`, `AEay6p5LdQg`) импортированы через VISIONOS, по 2,4 МБ, без `.part`; воспроизведение получает прямую ссылку `googlevideo.com`. Временный тест удалён.
- Новый тестовый установщик: `build/test-release/Protogenix-Setup-1.0.0-test.exe` (`1.0.0+0`, 27,1 МБ).
- Правила:
  - `known-issues.md` — YouTube: обход, решение Orion, исправленный баг;
  - `dependencies.md` — youtube_explode: как обновлять, когда форкать;
  - `CLAUDE.md` — импорт и воспроизведение. Порядок выбора источника в `toAudioSource` там был описан неверно: локальный файл проверяется раньше YouTube.

**Почему:** без скачивания релиз выпускать нельзя. Решение Orion — остаться на YouTube (Яндекс Музыка — цензура и блокировки, SoundCloud — нишевый каталог).

**Осознанно не трогал:**
- Запасной путь Invidious: при сбое он по-прежнему пробуется дважды, это лишние секунды только в случае неудачи.
- Незакоммиченную правку Orion в `invidious_proxy_service.dart`.
- Автоматизацию (Dependabot, проверки на PR, автоподъём версии в `release.ps1`, ежедневная проверка скачивания) и зеркало для обновлений: предложены, ждут решения Orion. Ключи подписи в CI не отдавать — релиз остаётся одной командой на машине Orion.
- Файлы по 0 байт от прошлых попыток в `music` Orion: при повторном импорте они заменятся.

## 2026-09-12 — Разбор: в тестовой сборке не скачивается музыка

**Что:**
- Жалоба Orion: установленная тестовая сборка Windows находит треки, но не скачивает — ни по ссылке, ни из поиска.
- Ход разбора:
  - в папке `music` — файлы по 0 байт: папки и получение манифеста работают, данных нет;
  - пробники на Dart (youtube_explode 3.0.5, AOT-exe, запросы через прямой маршрут и через AmneziaVPN), затем тест с настоящим `ImporterService` — всё качало, но на `dQw4w9WgXcQ`;
  - лог установленного приложения (перезапуск с `Start-Process -RedirectStandardOutput`) показал ошибки на треке Orion: `androidVr` «not a bot», `ios` 403, `android` — поток пустой, `mweb` — нет потоков, Invidious 403;
  - пробник всех клиентов на треках Orion — то же на 3.0.5 и 3.1.0.
- Причина: YouTube с августа 2026 требует PO-токен (issues youtube_explode_dart #386, #389, #379), исправлений в релизах библиотеки нет. К установщику и сборке отношения не имеет, касается всех сборок и Android.
- Найден рабочий обход: клиент visionOS (неслитые PR #390/#391), ~1,4 МБ/с на обоих треках Orion. Подробности — `known-issues.md`.
- Попутно найдено:
  - баг работы с файлом в `_tryDownloadWithClient` (удаление при открытом `IOSink` на Windows, общий файл у параллельных импортов одного трека);
  - импорт SoundCloud — заглушка («SoundCloud в следующем обновлении»), а README и описание релиза утверждали обратное — исправлено, `CLAUDE.md` уточнён.
- Обзор замены YouTube для Orion: Audius проверен — API работает из Беларуси, но каталог из пользовательских загрузок (каверы, ремиксы), официального OST нет. Остальные варианты (SoundCloud, Яндекс Музыка, VK, Deezer, Jamendo) — по общим данным, не проверялись.
- Все временные пробники — в scratchpad; временный тест `test/_tmp_import_probe_test.dart` удалён.

**Почему:** без скачивания релиз выпускать нельзя — это основная функция приложения.

**Осознанно не трогал:**
- Код импорта и плеера не менял: план (visionOS первым, youtube_explode 3.1.0, починка работы с файлом, честное сообщение об ошибке) ждёт решения Orion, как и выбор стратегии источников.
- Файлы по 0 байт в `%LOCALAPPDATA%\Z43 Studios\Protogenix\music` не удалял — это данные Orion, уберутся вместе с починкой.

**Мои ошибки в разборе:** сначала исходил из замедления YouTube в России, хотя Orion в Беларуси; час проверял сеть и VPN на одном популярном видео, вместо того чтобы сразу взять трек из жалобы.

## 2026-09-12 — Тестовый установщик Windows, проверка номера сборки exe в скрипте релиза

**Что:**
- Собран тестовый установщик для Orion: `build/test-release/Protogenix-Setup-1.0.0-test.exe` (27,1 МБ). Собран из текущей рабочей копии, включая незакоммиченную правку Invidious, с `--build-number=0`: `ProductVersion` у exe — `1.0.0+0`. После публикации 1.0.0 (`+1`) установленная копия увидит обязательное обновление и пройдёт весь путь обновления вживую.
- `tool/release.ps1` проверяет, что `protogenix.exe` собран как `X.Y.Z+N` из pubspec. Дыра нашлась на этой самой сборке: номер exe скрипт не проверял, а exe с `+0`, оставшийся в `build/windows`, после публикации дал бы петлю обновлений.
- Правила: `release.md` (проверка exe, как собрать тестовую сборку для проверки обновления).

**Почему:** Orion хочет сам проверить Windows-версию до релиза.

**Осознанно не трогал:**
- Установщик сам не запускал — проверяет Orion.
- Сквозную проверку обновления до публикации не делал: приложение ищет манифест только на GitHub, а подменять адрес ради теста — лишний риск в самом опасном месте приложения.

## 2026-09-12 — Подробное описание релиза, текст для приложения по маркеру, разбор незакоммиченной правки Invidious

**Что:**
- `docs/release-notes/v1.0.0.md` переписан подробно: логотип, возможности по разделам, таблица «какой файл скачать», установка, обновления, приватность, что делать тем, у кого стояла тестовая версия (Android — переустановка; Windows — ручной перенос медиатеки из папки zip-сборки по `data.md`, под спойлером).
- `tool/release.ps1`: в приложение (баннер «Что нового») уходит только часть описания до `<!-- more -->` — без HTML, картинок и разметки markdown, ссылки остаются текстом. Если вышло длиннее 500 символов (баннер показывает только их), скрипт предупреждает. Проверено: для v1.0.0 в приложение попадает 182 символа; без маркера уходит весь текст.
- Удалён `repomix-output.xml` (снимок репозитория от 2026-06-27, 1,1 МБ, в git не был) — по решению Orion.
- Разобраны незакоммиченные правки Orion от 2026-06-28 (оба файла сохранены в одну секунду — похоже на «сохранить всё» в IDE):
  - `search_screen.dart` — только сдвиг отступа в одной строке, по смыслу ничего не меняет;
  - `invidious_proxy_service.dart` — переделка запасного пути: список инстансов с `api.invidious.io` (кэш 6 ч), новый статический список, Piped как последний шаг, отсев «мусорных» ответов, подробная обработка ошибок. Компилируется (release-сборки 2026-09-11 шли с ней). Два бага:
    1. `_isUnusableResponse` вызывается для JSON и ищет во **всём** теле ответа слова вроде «unavailable», «no longer», «this instance». Они встречаются в описаниях видео и в результатах поиска, так что правильный ответ отбрасывается, а инстанс выкидывается из списка.
    2. Если `api.invidious.io` недоступен, неудача не кэшируется: каждый вызов снова ждёт его до 10 с.
  - Живая проверка инстансов 2026-09-12: не работает ни один — ни из старого списка, ни из нового, ни Piped. Подробности — `known-issues.md`.
- Правила: `release.md` (шаг 2 «Как выпустить релиз»), `known-issues.md` (запасной путь Invidious/Piped), `CLAUDE.md` (Playback).

**Почему:** просьбы Orion: подробное и красивое описание релиза, удалить repomix, проверить незакоммиченные правки перед коммитом.

**Осознанно не трогал:** незакоммиченные правки Orion не коммитил, не откатывал и не исправлял — решение за ним. Запасной путь Invidious/Piped не чинил и не удалял — отдельная задача.

## 2026-09-11 — Подпись Android проверена, APK по архитектурам исправлены, скрипт релиза

**Что:**
- Release-ключ Orion работает: APK подписаны `CN=Z43 Studios`, SHA-256 сертификата записан в `release.md` и в `tool/release.ps1`.
- **Найдено и исправлено:** `flutter build apk --target-platform <одна ABI>` клал в APK `.so` плагинов (libmpv, libsqlite3) под все три архитектуры: Flutter сам выставляет `abiFilters` на все ABI. Такой APK ставится на телефон другой архитектуры и падает при запуске. `android/app/build.gradle.kts` теперь вырезает лишние ABI при упаковке (`packaging.jniLibs.excludes`). arm64-APK: 63,9 → 36,8 МБ.
- Прежний «universal» APK от 2026-06-29 (67,3 МБ) на деле содержал движок Flutter только под arm64; замер в `release.md` исправлен. Настоящий universal — 105,2 МБ.
- `tool/release.ps1` — сборка и публикация релиза:
  - проверки: версия из pubspec; нет незакоммиченных изменений, включая новые неотслеживаемые файлы в `lib/`, `android/`, `windows/` и т. п.; `build` новее опубликованного; для `-Publish` — `gh`, тег, запушенный коммит;
  - три APK, у каждого проверяются подпись, versionCode, точный набор ABI с `libflutter.so`/`libapp.so`, выравнивание под 16 КБ;
  - установщик Windows, манифест, подпись, `gh release create`, сверка опубликованного манифеста;
  - `notes` для баннера переводятся из markdown в простой текст.
- `docs/release-notes/v1.0.0.md` — черновик описания первого релиза.
- Сухой прогон без публикации (`-AllowDirty`, одноразовый ключ подписи из scratchpad): код 0; arm64-v8a 36,8 МБ, armeabi-v7a 34,0 МБ, universal 105,2 МБ, установщик 27,1 МБ; манифест подписан.
- Мигратор Flutter сам дописал в `android/gradle.properties` `android.builtInKotlin=false` и `android.newDsl=false` — описано в `known-issues.md`.
- Правила: `release.md` (сертификат, «Как выпустить релиз», размеры, `--split-per-abi`, статус плана), `known-issues.md` (флаги мигратора), `CLAUDE.md` (команды).

**Почему:** шаги 4–5 плана. Проверка подписи подтвердила, что ключ работает, а проверка APK по архитектурам показала, что без исправления они не запускались бы на телефонах других архитектур.

**Осознанно не трогал:**
- `-Publish` не запускал: нет `gh`, код не закоммичен, публикация — решение Orion.
- Настоящий ключ подписи манифеста в прогоне не использовал. Файлы в `build/release/v1.0.0/` подписаны тестовым ключом, приложение их не примет; `-Publish` всё пересоберёт.
- `libmpv.so` на Android (11–15 МБ на ABI, media_kit там не нужен) не убирал — отдельная задача (`dependencies.md`).
- Миграцию на встроенный Kotlin не делал.
- Незакоммиченные правки Orion (`invidious_proxy_service.dart`, `search_screen.dart`) не трогал. Перед релизом их надо закоммитить или отложить.

## 2026-09-11 — Ключ обновлений вписан, установщик проверен, подпись Android подготовлена

**Что:**
- Публичный ключ `z43-2026` (создал Orion) вписан в `kUpdateSigningKeys`, проверка обновлений включена. Добавлен тест, что встроенные ключи корректны (по 32 байта).
- Установщик Windows собран (`Protogenix-Setup-1.0.0.exe`, 27,1 МБ) и прогнан на машине Orion:
  - тихая установка;
  - отказ ставить, пока приложение не закрывается (через ~16 с, код 1);
  - настоящее обновление с перезапуском приложения;
  - удаление с сохранением данных.

  Всё сработало. Установка из теста удалена.
- Живая проверка переноса баз: release-exe, запущенный из папки проекта, скопировал dev-базы в `%LOCALAPPDATA%\Z43 Studios\Protogenix\databases`. Защита от второго экземпляра сработала: вторая копия закрылась сама.
- `android/app/build.gradle.kts` подписывает release ключом из `android/key.properties`; без него — debug-ключом с предупреждением. Создана заготовка `android/key.properties` без паролей (файл в `.gitignore`).
- Правила: `release.md` (подпись Android, статус плана, результаты проверки установщика), `data.md` (ограничение переноса для zip-сборок), `known-issues.md` (предупреждение Kotlin Gradle Plugin).
- Проверки: `flutter test` — 53 пройдено, 1 пропущен; `gradlew app:signingReport` — release пока на debug-конфигурации (keystore ещё не создан).

**Почему:** шаги 2 и 3 плана. Установщик нельзя было проверить без Inno Setup, а подпись Android требует ключа, который создаёт Orion.

**Осознанно не трогал:**
- Keystore для Android не создавал — пароли должны быть только у Orion. Дал ему пошаговую инструкцию.
- Откат установщика при сбое посреди установки не проверял (нечем надёжно вызвать сбой); полагаемся на Inno Setup.
- Импорт библиотеки из старой zip-папки не делал: описал ручной способ в `data.md`, делать ли UI — решение Orion.
- Предупреждение Flutter про Kotlin Gradle Plugin не чинил: оно было и до этого и сейчас сборку не ломает.

## 2026-09-11 — Шаг 2 плана: установщик Windows (написан, ещё не собран)

**Что:**
- `windows/installer/protogenix.iss` — установщик на Inno Setup 6:
  - ставит программу в профиль пользователя без прав администратора (`%LOCALAPPDATA%\Programs\Protogenix`);
  - создаёт ярлыки и деинсталлятор, интерфейс на русском;
  - ждёт закрытия приложения по мьютексу, в тихом режиме до 15 секунд;
  - удаляет `{app}\data` и DLL прошлой версии;
  - с `/update=1` перезапускает приложение;
  - `AppId` задан раз и навсегда.
- `windows/runner/main.cpp` — защита от второго экземпляра через именованный мьютекс (в debug выключена): повторный запуск показывает уже открытое окно.
- `tool/build_windows_installer.ps1` — собирает приложение и установщик, версию берёт из pubspec, печатает SHA-256 результата.
- Правила: `release.md` (шаг 3 плана), `CLAUDE.md` (команды).
- Проверки:
  - `flutter build windows --release` с новым `main.cpp` — успешно (135 с);
  - BOM в `.iss` и `.ps1` на месте, `.ps1` разбирается без ошибок.

**Почему:** старое обновление копировало файлы поверх без отката и зависело от того, куда пользователь распаковал zip. Установщик даёт постоянную папку, откат при сбое и тихое обновление.

**Осознанно не трогал:**
- Установщик не собран и не проверен: на машине нет Inno Setup, его ставит Orion. Не проверены тихое обновление поверх, откат, запуск после `/update=1` и удаление.
- Защиту от второго экземпляра вручную не проверял — GUI не запускал.
- Данные пользователя при удалении программы не удаляются — сознательно.

## 2026-09-11 — Шаг 1 плана: ядро обновлений с подписанным манифестом

**Что:**
- Новый апдейтер в `lib/features/updater/`:
  - `update_manifest.dart` — формат манифеста и проверка полей: https-адреса, размер, SHA-256, `build`;
  - `update_envelope.dart` — подписанная обёртка `protogenix-update.json` и проверка Ed25519;
  - `update_checker.dart` — загрузка манифеста с GitHub Releases, сравнение по `build`, выбор файла по платформе и ABI;
  - `update_downloader.dart` — докачка, переход на следующий адрес, SHA-256 в изоляте;
  - `update_installer.dart` — на Android системный установщик, на Windows тихий запуск Inno Setup;
  - `update_provider.dart` и `update_banner.dart` переписаны под новую схему; обязательное обновление скрыть нельзя.
- Удалены `updater_service.dart` и `auto_updater.dart` (GitHub API и `.ps1`/`.sh`, копировавший файлы поверх).
- `tool/update_signing.dart` создаёт ключ и подписывает манифест. Писать ключ в репозиторий и перезаписывать существующий он отказывается (проверено).
- Зависимости: `crypto` стал прямой, добавлен `cryptography` 2.9.0 (Ed25519, чистый Dart). `audiotags` по-прежнему 1.1.3.
- 30 тестов в `test/features/updater/`, с локальным `HttpServer` вместо сети.
- Правила: `release.md` (схема обновлений, формат релиза, ключ подписи), `security.md` (раздел «Обновления»), `known-issues.md`, `CLAUDE.md`.
- Проверки:
  - `flutter test` — 52 пройдено, 1 пропущен;
  - `flutter analyze` — 8 замечаний (базовый уровень);
  - утилита подписи прогнана на одноразовом ключе во временной папке.

**Почему:** старая схема не видела релизы (404), никак не проверяла скачанное и на Windows копировала файлы поверх без отката.

**Осознанно не трогал:**
- Боевой ключ подписи не создавал: приватная часть должна быть только у Orion. Пока `kUpdateSigningKeys` пуст, обновления выключены.
- Установщик Inno Setup — шаг 2. Установка на Linux не поддерживается, там баннер ведёт на страницу релиза.
- На Android установка осталась через `OpenFile` (системный установщик); установка в один тап через `PackageInstaller` — позже.

## 2026-09-11 — Хостинг релизов: GitHub; данные Windows переехали в AppData

**Что:**
- Решение по хостингу: только GitHub Releases, манифест — ассет релиза (`release.md`). Yandex Object Storage отпал, потому что без платёжного аккаунта бакет не создать. Vercel Blob, GitFlic и Яндекс/Google Диск разобраны и отклонены.
- Новый `AppPaths` (`lib/core/services/app_paths.dart`) собирает все пути к базам и файлам в одном месте:
  - Windows — `%LOCALAPPDATA%\Z43 Studios\Protogenix`; путь явный и не зависит ни от рабочей папки, ни от `Runner.rc`;
  - Linux — `getApplicationSupportDirectory()`;
  - Android, iOS и macOS — как раньше.
- `LegacyDatabaseMigration` при первом запуске копирует базы со старого места (`.dart_tool/sqflite_common_ffi/databases` относительно рабочей папки и папки exe):
  - существующие базы на новом месте не перезаписывает;
  - копирует через временный файл вместе с `-journal`/`-wal`/`-shm`;
  - после успешного переноса ставит маркер;
  - при сбое читает старую папку и повторяет перенос при следующем запуске.
- На `AppPaths` переведены `LibraryDatabase`, `PlaylistDatabase`, `ImporterService` (музыка и обложки), `LyricsService.saveLrc` и кэш `TrackModel`. `AppPaths.init()` вызывается в `main()` до первого обращения к базам.
- 8 тестов переноса: `test/core/services/legacy_data_migration_test.dart`.
- Обновлены `data.md`, `release.md`, `known-issues.md`, `CLAUDE.md`.
- Проверки: `flutter test` — 22 пройдено, 1 пропущен; `flutter analyze` — 8 замечаний (базовый уровень).
- Убрал из индекса git случайно добавленную папку `яндекс/` со скриншотами, файлы остались на месте.

**Почему:** без постоянной папки данных нельзя делать установщик — обновление или удаление программы снесло бы базу, лежавшую рядом с exe. Заодно пропала зависимость от того, откуда запущено приложение, и `flutter clean` больше не стирает dev-базу.

**Осознанно не трогал:**
- Уже скачанные файлы (музыка, обложки, тексты в «Документах») не переносил: пути в базе абсолютные и продолжают работать, а перенос гигабайт при первом запуске — лишний риск. Новые файлы идут в новую папку.
- Старые базы после копирования не удаляю, они остаются резервной копией.
- Приложение не запускал. Проверку переноса на настоящей dev-базе делает Orion (`flutter run -d windows`).
- Пути на Android не менялись.

## 2026-09-11 — Телеметрия удалена, брендинг Z43 Studios, README, решения по релизу

**Что:**
- Удалил `TelemetryService` (файл и вызов в `main()`). Проверено: в `data/app.so` свежей Windows-сборки нет ни токена, ни `api.telegram.org`.
- Метаданные exe (`windows/runner/Runner.rc`): `CompanyName` = `Z43 Studios`, `ProductName` и `FileDescription` = `Protogenix`, копирайт Z43 Studios. Заголовок окна в `main.cpp` — `Protogenix`. Проверено по свойствам собранного exe.
- Новый `README.md`: возможности, установка (Android/Windows, «неизвестные источники», SmartScreen), сборка из исходников, технологии, дисклеймер.
- Обновил правила:
  - `CLAUDE.md`: платформы, публичный репозиторий, запуск без телеметрии;
  - `release.md`: платформы, Google Play, согласованный план установщика и обновлений;
  - `data.md`: папка данных Windows зависит от `CompanyName`/`ProductName`, пути в базе абсолютные;
  - `security.md`: репозиторий публичный;
  - `known-issues.md`: телеметрия удалена.
- Проверки:
  - скан секретов по дереву и истории git: единственная находка — токен бота из `3957a7e`;
  - `flutter analyze` — 8 замечаний (базовый уровень);
  - `flutter test` — 14 пройдено, 1 пропущен;
  - `flutter build windows --release` — успешно.

**Почему:** решения Orion:
- релиз на Android и Windows, Apple — нет, Linux под вопросом;
- закрытый тест в Google Play, поэтому без телеметрии;
- репозиторий стал публичным;
- нужны README и название студии.

**Осознанно не трогал:**
- `applicationId` (`z43.studios.protogenix`, уже содержит имя студии) — после первой загрузки в Play его не поменять, решение за Orion.
- `device_info_plus` оставил в `pubspec.yaml`: после удаления телеметрии он не используется, но понадобится для выбора APK по ABI.
- Отзыв токена бота — решение Orion (решил не беспокоиться).
- Скриншотов в README нет — нужны настоящие. Лицензию не выбирал.
- Установщик и обновления не начинал: сначала решения по Google Play (flavor'ы, ключ) и перенос данных Windows.

## 2026-09-11 — Аудит перед первым релизом: оптимизация и готовность

**Что:**
- Проверил релизный APK от 2026-06-29 (`aapt2`, `apksigner`, `zipalign -P 16`, `llvm-readelf`), собрал свежий Windows-релиз (`flutter build windows --release` — успешно, 83 МБ), прогнал `flutter pub outdated`, прошёлся по UI-коду, манифесту и конфигам iOS/macOS/Windows.
- Отчёт с приоритетами — в переписке с Orion. Новые факты записал в правила:
  - `release.md`: тегов нет, размеры сборок, 16 КБ — совместимо, iOS/macOS не готовы;
  - `performance.md`: фон + стекло, караоке, очередь, изображения;
  - `dependencies.md`: `audiotags` не используется, лишняя видео-часть media_kit;
  - `known-issues.md`: визуализатор — мёртвый код, `RECORD_AUDIO`; токен есть и в exe.

**Почему:** Orion готовит первый релиз и попросил оценить оптимизацию под устройства и понять, чего не хватает.

**Осознанно не трогал:**
- Код и `pubspec.yaml` — все исправления ждут приоритетов от Orion.
- APK не пересобирал, чтобы не затереть сборку от 2026-06-29; перед релизом его всё равно нужно собрать заново.
- Приложение не запускал: при старте уходит телеметрия в Telegram.

## 2026-09-10 — Документация: CLAUDE.md, правила `.claude/rules/`, этот журнал

**Что:**
- Создал `CLAUDE.md` (через `/init`), затем вынес детали по областям в `.claude/rules/`: `release.md`, `dependencies.md`, `performance.md`, `data.md`, `lyrics.md`, `security.md`, `known-issues.md`. У каждого файла есть `paths:` — он грузится, только когда читаются файлы его области. В CLAUDE.md остались общая картина, команды и таблица «какой файл правил когда читать».
- Завёл этот журнал.
- Перед записью проверил факты, а не переписал их с чужих слов:
  - `apksigner` на `app-release.apk`: подпись = debug-ключ этой машины;
  - запрос к API релизов: 404;
  - скрипт на порядок microtask и event loop;
  - скрипт на FK в sqflite: `PRAGMA foreign_keys = 0`;
  - поиск токена бота в `libapp.so`: найден;
  - `flutter analyze`: 8 замечаний; `flutter test`: 14 пройдено, 1 пропущен.

**Почему:** CLAUDE.md грузится каждую сессию, поэтому в нём только общее; детали подгружаются по месту. Формат `.jules/` («баг → урок», по темам, даты недостоверны) не подходит для хронологии, к тому же это рабочая память Jules. Несколько формулировок из исходного задания оказались неточными и записаны в исправленном виде:
- debug-keystore генерируется на каждой машине свой, а не одинаков у всех (`release.md`);
- `Future.microtask` не отпускает кадр (`performance.md`);
- каскад FK внутри базы плейлистов не работает (`data.md`).

**Осознанно не трогал:**
- Код — задача была только про документацию.
- Телеметрию с токеном бота — это вопрос к Orion (`known-issues.md`).
- Debug-подпись release и неработающий апдейтер (404) — решение за Orion (`release.md`).
- `Future.microtask` в `PlayerNotifier.loadPlaylist` и 13 мест `ref.watch(playerProvider)` без `select` — описаны в `performance.md`, но не переписаны.
- Импорт из поиска без `reloadFromLibrary()` — описан в `data.md`; «починка» прервала бы музыку при каждом импорте.
- `playback.md` не создавал: в `invidious_proxy_service.dart` лежит незакоммиченный diff почти на 500 строк, документировать код, который ещё меняется, рано.
- `.jules/` — только прочитал.
- `CLAUDE.md` оставил на английском (там текст Orion); правила и журнал — на русском, по просьбе Orion.
